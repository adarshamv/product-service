pipeline {
    agent {
        label 'product-catalogue-agent'
    }

    stages {
        stage('Test Agent') {
            steps {
                // run Maven version command here
            }
        }
    }
}
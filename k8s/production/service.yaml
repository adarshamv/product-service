apiVersion: v1
kind: Service
metadata:
  name: product-catalogue
  namespace: production

spec:
  type: ClusterIP

  selector:
    app: product-catalogue

  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP